# Assets ignored by `wrangler deploy` (see https://developers.cloudflare.com/workers/static-assets/binding/#ignoring-assets)
# Workers static assets do NOT auto-exclude .git etc. — list everything explicitly.

**/.git
**/.wrangler
README.md
DEPLOY.md
LICENSE.md
wrangler.jsonc
.gitignore
.assetsignore
scripts/**
workers/**